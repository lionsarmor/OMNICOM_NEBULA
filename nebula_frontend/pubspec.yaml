name: omnicom_frontend
description: "Nebula by OMNICOM — a retro-synthwave login and terminal interface."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  audioplayers: ^6.5.1   # 🎧 latest stable audio package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # === IMAGES ===
    - assets/images/
    # === SOUND EFFECTS ===
    - assets/sfx/
