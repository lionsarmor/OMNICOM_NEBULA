name: omnicom_frontend
description: "Nebula by OMNICOM — a retro-synthwave login and terminal interface."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # === CORE UI & ICONS ===
  cupertino_icons: ^1.0.8

  # === NETWORK & STORAGE ===
  http: ^1.5.0
  shared_preferences: ^2.3.3

  # === AUDIO / VIDEO / MEDIA ===
  audioplayers: ^6.5.1          # 🔊 sound effects
  video_player: ^2.9.2          # 🎥 plays MP4 / HLS / network streams
  webview_flutter: ^4.9.0       # ▶ renders YouTube embeds safely on all platforms

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sfx/
