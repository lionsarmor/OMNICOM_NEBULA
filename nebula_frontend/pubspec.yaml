name: omnicom_frontend
description: "Nebula by OMNICOM — a retro-synthwave login and terminal interface."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # === CORE UI & ICONS ===
  cupertino_icons: ^1.0.8

  # === NETWORK & STORAGE ===
  http: ^1.5.0
  shared_preferences: ^2.3.3

  # === AUDIO / VIDEO / MEDIA ===
  audioplayers: ^6.5.1
  video_player: ^2.9.2
  webview_flutter: ^4.9.0
  file_picker: ^8.1.3

  # === REAL-TIME SYNC (Watch Party) ===
  socket_io_client: ^2.0.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/sfx/
